# IMPORTANT: set tensorflow-cpu to the SAME version printed by
# save_for_deployment.py (tf.__version__ in Colab), otherwise the
# .keras file may fail to load.
# x86_64 (Hugging Face Spaces, Render, Intel Macs/PCs)
# tensorflow-cpu==2.19.0; platform_machine == "x86_64" or platform_machine == "AMD64"
# arm64 (Docker on Apple Silicon) — tensorflow-cpu has no arm64 wheels
tensorflow==2.20.0; platform_machine == "aarch64" or platform_machine == "arm64"
flask==3.0.3
gunicorn==22.0.0
numpy<2.1